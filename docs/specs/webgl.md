# WebGL 仕様メモ

WebGL は OpenGL ES ベースのブラウザ向けグラフィックス API です。

このプロジェクトでは、特別な理由がない限り WebGL 2 を優先します。WebGL 1 に対応する場合は、利用できない機能や代替実装を明記します。

## 主な確認対象

- コンテキスト初期化
- GLSL シェーダーのコンパイルとリンク
- 頂点バッファ、インデックスバッファ、uniform の管理
- テクスチャ、フレームバッファ、レンダーターゲット
- 拡張機能の有無とブラウザ差
- WebGPU 実装とのコード構造および性能上の違い

