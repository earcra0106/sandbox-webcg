# refactor/allow-git-push

## 作業の目的と概要

`AGENTS.md` の作業ルールを更新し、ユーザーが明示的に許可または依頼した場合に `git push` などのリモート操作を実行できるようにする。

## 作業方針

- `git push` などのリモート操作を絶対禁止する文言を変更する。
- リモート操作は、ユーザーが明示的に許可または依頼した場合のみ実行できるルールにする。
- 変更後にコミットし、ユーザーの依頼に従って作業ブランチを push する。

## 作成・編集するファイルのスコープ

- `AGENTS.md`
- `docs/tasks/20260520_1506_allow-git-push.md`

## 実装する関数、クラス、モジュールの概要

コード実装は行わない。ドキュメント更新のみを対象とする。

## URLパスやUI操作

該当なし。アプリケーション実装はまだ存在しない。

## Git 作業メモ

- 作業ブランチ: `refactor/allow-git-push`
- 作業開始時のブランチ: `main`
